% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geneset_diff_cal.R
\name{geneset_diff_cal}
\alias{geneset_diff_cal}
\title{Train Survival Models Using Multiple Machine Learning Algorithms}
\usage{
geneset_diff_cal(
  gs.exp,
  grouping,
  prefix,
  geneset_name,
  method = NULL,
  normalized = T,
  outdir = "./"
)
}
\arguments{
\item{gs.exp}{Gene expression matrix (genes as rows, samples as columns)}

\item{grouping}{Dataframe with sample grouping information (must contain 'accession' and 'group' columns)}

\item{prefix}{Prefix string for output filenames}

\item{geneset_name}{Name of the gene set being analyzed}

\item{method}{Analysis method: "limma" for linear models or alternative statistical method (default: "limma")}

\item{normalized}{Logical indicating whether to normalize expression data (default: TRUE)}

\item{outdir}{Output directory path (default: "./")}
}
\value{
Invisibly returns a list containing:
\itemize{
  \item For limma method: List of differential expression results for each contrast
  \item For other methods: Dataframe with mean expression and p-values
}
}
\description{
Performs differential analysis on gene sets and trains survival models using
various machine learning approaches. Supports both limma and basic statistical methods.
}
\examples{
\dontrun{
# Using limma method
result <- geneset_diff_cal(
  gs.exp = expression_matrix,
  grouping = sample_groups,
  prefix = "analysis1",
  geneset_name = "hallmark_apoptosis"
)

# Using t-test method
result <- geneset_diff_cal(
  gs.exp = expression_matrix,
  grouping = sample_groups,
  method = "t.test",
  prefix = "analysis2",
  geneset_name = "my_geneset"
)
}
Standardize dataframe column names to comply with R naming conventions
}
